extends: [base.yaml, datasets/cora.yaml]
experiment: cora_mlp_fca
# Isolates the concept contribution from message passing (no graph used).
features:
  variant: fca_feat
  fca:
    k_concepts: 128
    membership: hard
model:
  name: mlp
  hidden_channels: 128
  num_layers: 2
