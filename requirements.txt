# Core scientific stack
numpy>=1.23,<2.0
pandas>=1.5
scipy>=1.10
scikit-learn>=1.2
PyYAML>=6.0
matplotlib>=3.6

# Deep learning / GNN stack
# Install torch first (CPU example):
#   pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
# then:
#   pip install torch_geometric
torch>=2.1
torch_geometric>=2.4

# ---- Optional extras (uncomment if needed) ----
# Large OGB benchmark (ogbn-arxiv):
# ogb>=1.3.6
# FCA mining backends (the repo ships a self-contained fallback miner,
# so these are optional and only used if importable):
# caspailleur>=0.2
# paspailleur>=0.1
# FCApy is only used for optional lattice visualisation:
# fcapy>=0.1.4
